# connector.name=iceberg
# iceberg.catalog.type=jdbc

# # Postgres for the Iceberg catalog (the Airflow Postgres container)
# iceberg.jdbc-catalog.driver-class=org.postgresql.Driver
# # iceberg.jdbc-catalog.connection-url=jdbc:postgresql://airflow-docker-postgres-1:5432/iceberg_catalog
# # iceberg.jdbc-catalog.connection-url=jdbc:postgresql://airflow-docker-postgres-1:5432/iceberg_catalog?options=-c%20search_path%3Diceberg%2Cpublic
# iceberg.jdbc-catalog.connection-url=jdbc:postgresql://airflow-docker-postgres-1:5432/iceberg_catalog?currentSchema=iceberg
# iceberg.jdbc-catalog.connection-user=etl
# iceberg.jdbc-catalog.connection-password=demopass
# iceberg.jdbc-catalog.catalog-name=catalog_dw
# iceberg.jdbc-catalog.default-warehouse-dir=s3://market
# # iceberg.jdbc-catalog.catalog-schema=iceberg

# # MinIO S3
# fs.hadoop.enabled=false
# fs.native-s3.enabled=true
# s3.endpoint=http://minio:9000
# s3.region=us-east-1
# s3.aws-access-key=minioadmin
# s3.aws-secret-key=minioadmin
# s3.path-style-access=true

connector.name=iceberg
iceberg.catalog.type=hive_metastore
hive.metastore.uri=thrift://hive-metastore:9083

# Trino native S3 client (works with MinIO)
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.path-style-access=true
s3.region=us-east-1
s3.aws-access-key=minioadmin
s3.aws-secret-key=minioadmin

iceberg.file-format=PARQUET